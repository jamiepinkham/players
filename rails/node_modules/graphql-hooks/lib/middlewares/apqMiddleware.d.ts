import { MiddlewareFunction } from '../types/common-types';
export declare function sha256(query: any): Promise<string>;
declare type APQExtension = {
    persistedQuery: {
        version: number;
        sha256Hash: string;
    };
};
/**
 * AutomaticPersistedQueryMiddleware - must be last in the middleware list
 * @param {function} makeRequest
 * @returns Promise<object>
 */
export declare const APQMiddleware: MiddlewareFunction<APQExtension>;
export {};
