# Set timezone to Eastern Time (automatically handles EST/EDT)
CRON_TZ=America/New_York

# Convert leading bids to contracts nightly at midnight Eastern Time
0 0 * * * cd /app && bundle exec rake convert_bids:convert_leading >> /proc/1/fd/1 2>&1
