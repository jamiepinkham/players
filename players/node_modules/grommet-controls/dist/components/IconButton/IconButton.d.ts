import { ButtonProps } from 'grommet';
import { Omit } from '../../utils';
/**
 * A Button with a hover effect, all properties of Button apply.
 */
declare const IconButton: (props: ButtonProps & Omit<JSX.IntrinsicElements['button'], 'color' | 'ref'>) => JSX.Element;
export { IconButton };
