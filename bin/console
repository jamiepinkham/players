#!/usr/bin/env bash
# Quick Rails console access
# Usage: ./bin/console

set -euo pipefail

# Change to project root
cd "$(dirname "$0")/.."

docker compose run --rm players bundle exec rails console
